---
description: Apply the most recent 16 Eyes findings — safe ones directly, risky ones with your confirmation. Never commits or pushes.
mode: agent
---

Use the `@16-eyes-fix` custom agent to apply the most recent `@16-eyes-audit` or
`@16-eyes-audit-diff` findings. If `@16-eyes-fix` isn't available as a custom agent in
this Copilot surface, follow `.github/agents/16-eyes-fix.agent.md`'s instructions
directly instead.
