---
description: Configure 16 Eyes and design this repo's security investigation lenses.
mode: agent
---

Use the `@16-eyes-init` custom agent to configure 16 Eyes for this repository (detect
quality gates/excludes/output, then profile the repo and design its persisted
investigation lenses). If `@16-eyes-init` isn't available as a custom agent in this
Copilot surface, follow `.github/agents/16-eyes-init.agent.md`'s instructions directly
instead.
